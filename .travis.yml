language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest tests # or py.test for Python versions 3.5 and below
  - pylint osscie | pylint_output.txt
after_success:
  - coveralls
